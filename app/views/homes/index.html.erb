<%= link_to t(".import_file"), file_ceaps_path, class: "btn btn-primary", style: "float:right;" if user_signed_in? %>
<br/>
<%= render "search" %>
<hr>
<%= render "table" if @deputes.exists? %>
<div class="pagination justify-content-center">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
