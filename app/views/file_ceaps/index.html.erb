<%= link_to t(".new_import"), new_file_ceap_url, remote: true, style: "float: right;", class: "btn btn-primary"%>
<br/>
<%= render "search" %>
<hr>
<%= render "table" if @file_ceaps.exists? %>

<%= render 'modal' %>

<div class="pagination justify-content-center">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>